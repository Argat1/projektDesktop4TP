<Window x:Class="projektDesktop4TP.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:projektDesktop4TP"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Grid.Row="0" Height="50" Width="150" Grid.ColumnSpan="2" VerticalAlignment="Bottom" Content="Zaloguj" Click="Button_Click"/>
        <Button Grid.Column="0" Grid.Row="1" Height="50" Width="150" VerticalAlignment="Top" Content="Zarejestruj jako Pracownik" Click="Button_Click_2"/>
        <Button Grid.Column="1" Grid.Row="1" Height="50" Width="150" VerticalAlignment="Top" Content="Zarejestruj jako Pracodawca" Click="Button_Click_3"/>
    </Grid>
</Window>
